import { StyleSheet } from "react-native";

export const chartWidgetStyle = StyleSheet.create({
    container: {
        backgroundColor: 'white',
        borderRadius: 8,
        overflow: 'hidden',
        justifyContent: 'center',
        alignItems: 'center',
    },
    chartStyle: {
        borderRadius: 8,
        alignSelf: 'center',
    },
    chartConfig: {
        borderRadius: 8,
    },
});
